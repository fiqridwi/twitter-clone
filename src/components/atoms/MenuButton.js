import React from "react";

const MenuButton = () => {
  return (
    <div className="flex justify-center mt-5 px-5 ">
      <button className="bg-primary rounded-full px-20 text-white font-bold py-3 w-screen">
        Tweet
      </button>
    </div>
  );
};

export default MenuButton;
